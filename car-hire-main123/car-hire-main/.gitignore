# Environment files (NEVER commit these - they contain secrets!)
.env
.env.local
.env.*.local
backend/.env
backend/.env.*
server/.env
server/.env.*

# Dependencies
node_modules/
backend/node_modules/
server/node_modules/

# IMPORTANT: DO NOT ignore package-lock.json
# Netlify needs this file to install the exact package versions

# Logs
*.log
backend/backend.log
backend/server.log

# Build output (generated during build, not source code)
dist/
build/

# Editor and OS files
.DS_Store
.vscode/settings.json
*.swp
*.swo
*~
